<article class="m-3 post">

  <div>
    <h3 class="fs-4 fw-bold text-decoration-underline">{{title}}</h3>
    <p class="fs-5 my-4">{{post_text}}</p>
  </div>
  <div class="fw-semibold">
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</article>